from util.logging import Logger

import measurements.po4.wod.data.values
import measurements.util.plot


with Logger():
    filename = '/tmp/wod13_po4_distribution_year_{}.pdf'
    
    for time_len in (365, 2880, 48):
        measurements.util.plot.distribution_year(measurements.po4.wod.data.values.measurement_dict_unsorted(), file=filename.format(time_len), time_step=1./time_len)